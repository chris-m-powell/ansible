---
- block:
    - import_tasks: "install.yml"
      tags:
        - ranger-install
    - import_tasks: "configure.yml"
      tags:
        - ranger-configure
    - import_tasks: "validate.yml"
      tags:
        - ranger-validate
  rescue:
    - meta: flush_handlers
  tags:
    - ranger
...
