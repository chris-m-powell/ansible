package:
  zsh:
    installed: true
  zsh-autosuggestions:
    installed: true
  zsh-syntax-highlighting:
    installed: true
file:
  {{ [ansible_facts.user_dir,'.zshrc'] | join ('/') }}:
    exists: true
    owner: {{ user }}
    group: {{ user }}
    mode: "0640"
    filetype: file
